- if @notification.user.first_name.blank?
  Hello:
- else
  Hello, #{@notification.user.first_name}:#{%Q{\r\n}}

#{%Q{\r\n}}
- if @notification.app.present?
  The "#{@notification.app.name}" MyUSA application has sent you the following message:#{%Q{\r\n}}
- else
  A notification for you from MyUSA:#{%Q{\r\n}}
#{%Q{\r\n}}
===============================================#{%Q{\r\n}}
#{%Q{\r\n}}
= strip_tags(@notification.body)