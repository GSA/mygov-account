.row
  .twelve.columns
    %h2
      = t(:change_your_password)
    .eight.columns
      .panel
        = form_for(@user, :url => { :action => "update_password" } ) do |f|
          = error_messages_for(@user)
          %div
            = f.label :password, t(:new_password)
            %br/
            = f.password_field :password, autocomplete: 'off', :tabindex => autotab
          %div
            = f.label :password_confirmation, t(:confirm_new_password)
            %br/
            = f.password_field :password_confirmation, autocomplete: 'off', :tabindex => autotab
          %div
            = f.submit t(:change_my_password), class: 'large button positive', :tabindex => autotab, :accesskey => access_keys[:submit]
            = link_to 'Cancel', account_index_path, :tabindex => autotab
