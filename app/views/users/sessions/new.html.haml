.col-md-12
  %h2
    = t(:sign_in)
  = flash_messages
  = devise_error_messages!

.row
  .col-md-7
    = form_for(resource, :as => resource_name, :url => session_path(resource_name), html: {novalidate: true}) do |f|
      = flash_messages
      = devise_error_messages!
      %fieldset.plain
        %ol.inline.sign-in-panel
          %li.row
            .col-md-2
              = f.label :email
            .col-md-2
              = f.email_field :email, :tabindex => autotab
          %li.no-margin.row
            .col-md-2
              = f.label :password
            .col-md-2
              = f.password_field :password, autocomplete: 'off', :tabindex => autotab
          - if devise_mapping.rememberable?
            %li.row
              .col-md-2
              .col-md-6
                = f.check_box :remember_me, :tabindex => autotab
                = f.label :remember_me

                - if devise_mapping.recoverable? && controller_name != 'passwords'
                  %br
                  = link_to t(:forgot_your_password), new_password_path(resource_name), :tabindex => autotab


                .actions
                  = f.submit t(:sign_in), :tabindex => autotab, :class=>'btn btn-primary'
                  - if devise_mapping.registerable? && controller_name != 'registrations'
                    or
                    %strong
                      = link_to "Sign up for a new account", :tabindex => autotab, new_registration_path(resource_name)

  = render 'shared/signup_options'
