!!!
%html
  %head
    %title MyGov
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    %h1 
      = link_to 'MyGov', root_path
    - if current_user
      %p Hello, #{link_to current_user.name, profile_path}!
      %p= link_to "You have #{current_user.messages.count} messages.", messages_path
      %p= link_to "Sign out", destroy_user_session_path
    - else
      %p= sign_in_with_google_link
    = yield
